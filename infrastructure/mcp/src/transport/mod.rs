// Dummy — actual transport code is in ../transport.rs
// This file exists because Rust requires either transport.rs OR transport/mod.rs.
// The real transport module is in transport.rs which is compiled via the parent.
