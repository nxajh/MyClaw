//! Orchestrator stub.

/// Orchestrator stub — to be implemented.
pub struct Orchestrator;

impl Orchestrator {
    pub fn new() -> Self {
        Orchestrator
    }
}

impl Default for Orchestrator {
    fn default() -> Self {
        Self::new()
    }
}
